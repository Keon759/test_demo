# 测试用例设计

| 用例编号 | 模块 | 标题 | 前置条件 | 测试数据 | 预期结果 | 优先级 |
| --- | --- | --- | --- | --- | --- | --- |
| TC-REG-001 | 注册 | 合法用户注册成功 | 用户名未存在 | username=`tester01`, password=`secret123` | 返回注册成功，状态码 201 | P0 |
| TC-REG-002 | 注册 | 用户名为空不可注册 | 无 | username=``, password=`secret123` | 返回用户名和密码不能为空，状态码 400 | P0 |
| TC-REG-003 | 注册 | 密码为空不可注册 | 无 | username=`tester02`, password=`` | 返回用户名和密码不能为空，状态码 400 | P0 |
| TC-REG-004 | 注册 | 短密码不可注册 | 无 | username=`tester03`, password=`123` | 返回密码长度不能少于6位，状态码 400 | P0 |
| TC-REG-005 | 注册 | 短用户名不可注册 | 无 | username=`abc`, password=`secret123` | 返回用户名长度不能少于6位，状态码 400 | P0 |
| TC-REG-006 | 注册 | 重复用户名不可注册 | 用户名已注册 | username=`tester01`, password=`secret123` | 返回用户名已存在，状态码 409 | P0 |
| TC-LOG-001 | 登录 | 合法账号登录成功 | 用户已注册 | username=`tester01`, password=`secret123` | 返回登录成功，状态码 200 | P0 |
| TC-LOG-002 | 登录 | 错误密码不可登录 | 用户已注册 | username=`tester01`, password=`wrong123` | 返回用户名或密码错误，状态码 401 | P0 |
| TC-LOG-003 | 登录 | 未注册用户不可登录 | 用户不存在 | username=`ghost01`, password=`secret123` | 返回用户名或密码错误，状态码 401 | P0 |
| TC-LOG-004 | 登录 | 登录用户名为空 | 无 | username=``, password=`secret123` | 返回用户名和密码不能为空，状态码 400 | P0 |
